dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "DestinationBuilder"

if (!System.env.JITPACK)
    include ':app'

include ':destination-builder'
